apiVersion: v1
kind: Service
metadata:
  name: postgres-users
  namespace: rednorte
spec:
  selector:
    app: postgres-users
  ports:
    - port: 5432
      targetPort: 5432
---
apiVersion: v1
kind: Service
metadata:
  name: postgres-waitlist
  namespace: rednorte
spec:
  selector:
    app: postgres-waitlist
  ports:
    - port: 5432
      targetPort: 5432
---
apiVersion: v1
kind: Service
metadata:
  name: notifications-postgres
  namespace: rednorte
spec:
  selector:
    app: notifications-postgres
  ports:
    - port: 5432
      targetPort: 5432
